sudo: required
services:
  - docker

before_install:
  - docker build -t damianlis/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true USERNAME/react-test npm test

after_success:
  - docker build -t damianlis/multi-client ./client
  - docker build -t damianlis/multi-nginx ./nginx
  - docker build -t damianlis/multi-server ./server
  - docker build -t damianlis/multi-worker ./worker
